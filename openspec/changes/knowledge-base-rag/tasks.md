## 1. 架构与基础配置

- [x] 1.1 新增 RAG 向量索引与数据一致性 ADR，记录 pgvector 本期决策、策略端口和 Qdrant/Redis 重评估条件，并通过文档检查确认关联 ADR 完整
- [x] 1.2 在后端加入经核对兼容的 LangChain4j、OpenAI 兼容模型、pgvector JDBC/解析依赖，并运行 `mvn dependency:tree` 验证无冲突
- [x] 1.3 新增 Chat/Embedding/索引后端配置属性与环境变量占位，验证缺少密钥时 readiness 明确失败且配置文件不含真实凭证
- [x] 1.4 增加日志敏感字段脱敏与配置校验测试，验证 API Key 和 Authorization 不出现在日志及异常响应

## 2. 数据库与用户隔离模型

- [x] 2.1 新增 Flyway 迁移创建 `vector` 扩展及知识库、文档、原文件、分块、索引任务/版本表，并在本地 Postgres 上验证迁移成功
- [x] 2.2 在迁移中创建会话、会话知识库、消息和摘要表，验证外键、顺序约束与删除行为符合设计
- [ ] 2.3 为 owner、知识库、文档、状态、全文检索和向量查询创建必要索引，并用 `EXPLAIN` 验证高频查询命中索引
- [ ] 2.4 建立 knowledge 领域 entity/mapper/DTO，运行 mapper 测试验证 owner 条件和组合关联约束
- [ ] 2.5 建立 chat 领域 entity/mapper/DTO，运行 mapper 测试验证会话和消息按 owner 隔离并保持顺序

## 3. 知识库管理后端

- [x] 3.1 实现知识库创建、列表、详情、编辑服务，验证所有入口从认证上下文取得 userId 且跨用户资源表现为不存在
- [x] 3.2 实现文本录入和受限 TXT/Markdown/PDF 上传，验证 MIME、大小、空内容和文件名安全校验
- [ ] 3.3 实现文档列表、状态、失败原因和重试服务，验证任务状态转换合法且错误摘要不泄露敏感信息
- [ ] 3.4 实现文档与知识库幂等删除及级联清理，验证删除后立即不可检索且不影响另一用户的同名内容
- [x] 3.5 暴露知识库/文档 REST API 并编写 MVC 测试，覆盖成功、校验失败、未认证和跨用户场景

## 4. 模型与索引策略

- [x] 4.1 定义 `KnowledgeIndex`、查询/范围/结果模型和 backend 枚举，使用契约测试验证接口强制包含 owner 与知识库范围
- [x] 4.2 通过条件装配注册 pgvector 实现，并验证选择未实现的 Qdrant/Redis 时 fail-fast 而非静默回退
- [ ] 4.3 手动装配 OpenAI 兼容 Chat/Embedding 客户端，使用 stub server 验证超时、重试、错误映射和向量维度校验
- [x] 4.4 迁移并整理递归父子分块器，编写中英文、边界长度和重叠单元测试
- [x] 4.5 迁移中文 unigram/bigram 与拉丁词规范化器，验证入库和查询使用相同词项结果
- [ ] 4.6 实现 pgvector 幂等批量写入和按 owner/知识库/文档删除，运行数据库集成测试验证范围正确
- [ ] 4.7 实现 dense 与 lexical 两路候选及 RRF 混合查询，运行固定评测集验证关键词、语义和用户过滤结果
- [ ] 4.8 实现统一引用与父块去重组装，验证返回 chunk、文档、页码/位置和来源元数据

## 5. 持久化索引任务

- [ ] 5.1 实现索引任务创建、领取和并发锁定，验证多个 worker 不会重复消费同一任务
- [ ] 5.2 实现解析、分块、批量 Embedding、索引写入和 READY 状态流水线，并用 stub 模型跑通文本入库
- [ ] 5.3 实现失败重试、退避和可读错误摘要，验证进程重启后 pending/processing 任务可恢复
- [ ] 5.4 实现按 backend/model/version 的全量重建与校验入口，验证目标未 READY 时不能切换读取后端

## 6. 会话、分层记忆与流式问答后端

- [ ] 6.1 实现会话 CRUD、知识库多选和消息历史 API，验证所选知识库全部属于当前用户且 ready
- [x] 6.2 实现按 Token/容量预算读取最近消息的 L1 工作记忆，验证长会话不会把完整历史发送给模型
- [x] 6.3 实现 L2 滚动摘要和覆盖序号，使用 stub 模型验证摘要失败不丢失原始消息
- [ ] 6.4 使用 Redis 锁和取消标记保护同一会话生成，验证并发请求不会打乱消息顺序
- [ ] 6.5 实现查询改写、混合检索、提示注入隔离、模型调用和引用快照编排，验证无依据时返回未找到
- [ ] 6.6 实现 POST SSE token/done/error 协议及中断处理，使用 MVC 测试验证完成、取消、超时和异常事件
- [ ] 6.7 编写两个用户的问答集成测试，验证检索、历史、摘要、引用和删除全过程不串用户

## 7. 前端路由与知识库管理

- [x] 7.1 定义知识库、文档、会话、消息、引用和 SSE 事件的前端类型/API 客户端，使用 mock 响应验证解析
- [x] 7.2 将 MainLayout 改为路由驱动的一级菜单和页面标题，验证“知识库管理”“智能问答”“我的应用”切换及选中状态
- [x] 7.3 实现知识库列表、创建、编辑和删除交互，使用组件测试覆盖成功、空状态和错误提示
- [ ] 7.4 实现知识库详情、文本录入和 TXT/Markdown/PDF 上传，验证客户端大小/类型校验和上传反馈
- [ ] 7.5 实现文档处理状态、进度、失败重试和删除，使用 mock 轮询验证状态更新停止条件

## 8. 前端智能问答

- [ ] 8.1 实现会话列表、新建会话和知识库多选，验证不能提交未选择或不可用的知识库
- [ ] 8.2 使用 Ant Design X/Ant Design 实现消息气泡、输入框和空状态，验证历史消息与生成中状态正确渲染
- [ ] 8.3 使用 `fetch` + `ReadableStream` 接入 POST SSE，验证 token 增量、done、error 和断线处理
- [ ] 8.4 实现停止生成、重试和并发发送保护，验证取消后 UI 与后端消息状态一致
- [ ] 8.5 实现引用列表及文档/页码定位展示，验证被删除文档的历史引用以不可用状态显示
- [ ] 8.6 运行 `npm run typecheck`、前端测试和 `npm run build`，修复所有类型与构建问题

## 9. 端到端验证与安全收尾

- [ ] 9.1 在本地 Docker Postgres/Redis 上启动后端并验证 health、Flyway、vector 扩展与索引 worker 就绪
- [ ] 9.2 使用本地环境变量注入模型配置完成一次真实 Embedding 和 Chat 冒烟，验证任何命令输出和仓库文件均不包含 API Key
- [ ] 9.3 完成两个账号的上传、索引、混合检索、流式追问、摘要、删除端到端测试并保存非敏感测试结果
- [ ] 9.4 执行 Git diff、历史暂存区和常见密钥模式扫描，确认真实凭证、上传原文和本地运行数据未进入提交范围
- [ ] 9.5 运行后端完整测试、前端完整测试及构建，确认现有登录、我的应用和 MCP 闭环无回归
- [ ] 9.6 运行 `openspec validate knowledge-base-rag --strict` 并修复全部校验问题

## 验证状态说明（2026-08-30）

已完成项的含义：

- **2.1 / 2.2**：V8 迁移已在本地 Postgres 成功应用（表、`vector` 扩展、索引齐全），并用事务脚本验证
  删除知识库会级联清理文档 / 原文 blob / 分块 / 索引记录 / 任务。
- **3.1 / 3.2**：服务层已实现，跨用户隔离与参数校验有 MVC / 单元测试覆盖；真实文件上传的端到端尚未跑。
- **3.5**：KnowledgeBase / KnowledgeDocument 的 MVC 测试覆盖成功、校验失败、未认证与跨用户场景。
- **6.2 / 6.3**：分层记忆（L1 窗口 / L2 滚动摘要）新增单元测试，覆盖未达阈值不摘要、覆盖序号推进、
  摘要失败不改动 L0 消息、模型未配置时跳过。
- **7.1 / 7.2 / 7.3**：前端类型与 API 客户端、路由驱动菜单、知识库列表页已有 Vitest 覆盖（10 项通过）。

待运行时联调（代码已实现，缺真实环境验证）：索引任务流水线（5.x）、混合检索与引用（4.6-4.8）、
SSE 问答（6.5/6.6）、两用户集成测试（6.7）、文档处理状态与重试（3.3/3.4、7.4/7.5）、智能问答页（8.x）。
阻塞原因：本地 Postgres 容器冻结且 docker CLI 不可用，需重启容器运行时后补跑。
