## 1. 规划登记（本变更）
- [x] 1.1 登记 `agent-dev-loop`（合并 sandbox + ai-automation）/ `observability` 规划 capability
- [x] 1.2 明确与现有 `security` / `mcp-server` / `cross-border-erp-data` / `local-development` 的协同

## 2. 后续实现（待启动，各自独立 change）
- [ ] 2.1 `observability`：复用审计日志接入 tracing / metrics（建议优先）
- [ ] 2.2 `agent-dev-loop`：实现隔离沙箱 + 自动化编排 + 闭环交付流水线（建议后置）
