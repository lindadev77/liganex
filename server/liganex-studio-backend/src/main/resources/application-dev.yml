# dev 环境配置（可提交，不含任何真实密钥 —— ADR-0007）
logging:
  level:
    root: INFO
    tech.liganex: DEBUG
    org.flywaydb: INFO
    org.springframework.security: INFO

spring:
  datasource:
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
